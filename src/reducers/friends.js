import { } from "../types";

const initialState = {
  data: [],
  loading: false,
  errors: {}
};

export default function friends(state = initialState, action = {}) {
  switch (action.type) {
    default:
      return state;
  }
}
